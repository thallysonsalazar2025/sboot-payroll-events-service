version: "3.8"
services:
  payroll-events:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    volumes:
      - ./:/workspace
# Optional services for future integration (commented)
#  rabbitmq:
#    image: rabbitmq:3-management
#    ports:
#      - "5672:5672"
#      - "15672:15672"
#  localstack:
#    image: localstack/localstack
#    environment:
#      - SERVICES=s3
#    ports:
#      - "4566:4566"
